# Qt port code parity ledger

This ledger compares the current Tk behavior and geometry owners with the Qt
implementation. A source match must also survive rendered Mac and Windows
checks at the same **client** size and DPI. Tk's `window_logical_metrics().px`
and `ScenePainter` geometry, parent grid constraints, and the active route all
contribute to final bounds; matching one literal width is insufficient.

The complete code-file index is generated by
`engineering-quality/runners/qt-port-source-index.py` and currently records
519 files (148 Python modules and six QML files under `yt_downloader`, plus
tests, harness, scripts, packaging and CI). It records content hashes,
top-level Python symbols and local imports. The index makes omissions visible;
it is not a parity result. Each Tk-specific presentation owner and every
packaging/harness entry still needs the code-path and rendered checks below.

States: **wired** means an action reaches an existing owner, **rendered** means
the actual Qt scene was captured, and **open** means the feature or geometry has
not cleared parity. None of these labels claims package or installed proof.

| Surface | Tk source, controls and effective layout contract | Qt source and current result | Gate |
| --- | --- | --- | --- |
| Shared shell | `app.py` `_layout_focus_header`, `_select_focus_view`; four nav controls, global search and settings; compact header stacks when measured siblings cannot fit. `ui_button_contract.py` sets button height/font/icon metrics. | `Main.qml` single header row with `StoneButton`; `main.py:Materials` uses existing button image/field material. No measured header stacking yet. | Open: compare compact and wide bounds, search and focus reachability. |
| Forge command row | `app.py:_build_focus_forge_view`: 40 logical px URL field; Options and Download alongside, measured `arrange_command_row` moves actions under field when necessary. Format selector sits inside field. | `Main.qml` has URL/format/options/download in one row at 52/38/44 px. Actions remain on one row. | Open: match measured row breakpoints and control positions. |
| Forge second row | Tk Load URL list, then Save to label + 240 px `PillAction`, then Have local audio? + Create video; `arrange_local_row` stacks by measured required width. | `Main.qml` now has this order and a shared concave `StoneField` destination, but Qt width is adaptive 150–250 px and stacking is unverified. | Open: same client sizes and actual button/field bounds. |
| Forge activity and Run Deck | Tk live activity 3:2 columns, detail pane flexible; Run Deck is inset 20 px, tiles use `focus_run_deck_capacity`, overflow points to full runs. | `Main.qml` simplified live panel and `RunDeck.qml` projected active/queued/completed/terminal runs; Run Deck fixed 139 px. | Open: live title/artwork/details/technical modes, responsive card count, menus, positioning. |
| Library sidebar | `library_scene_ui.py` requests 226 px sidebar plus 20 px gap. `library_scene_layout.py:_draw_sidebar` places ARCHIVE at (14,30), five rows starting y=54 with 49 px stride and 45 px control height; bottom 124 px capacity tile at `max(343,height-141)`. Parent constraints can reduce actual canvas width. | `LibraryScene.qml` has five 45 px `StoneButton` rows at 4 px spacing, requested 226 px width and capacity tile through `StorageCapacityOwner`. Rendered Qt divider is farther right relative to the window than rendered Tk, so literal request parity is insufficient. | Open: trace parent constraint and compare actual divider/row/card bounds. |
| Library category tiles | `library_scene_layout.py:_categories` uses up to four columns, 14 px gaps, 106 px surfaces and a compact text branch at tile <220 px. | `LibraryScene.qml` four tiles and 106 px height; compact label/count branch and icon/subtitle missing. | Open: compact/wide geometry and details. |
| Library home collections | Tk up to five columns; 14 px gap; 192 px group card; Add Collection is dashed 192 px; collection/channel card artwork role differs. | `LibraryScene.qml` group/add cards now 192 px, but only four columns; dashed add, group overflow, variant summaries and artwork roles still differ. | Open: exact variants and bounds. |
| Library media browse | Tk `_toolbar`: 24 px heading, 200×40 search, 172 sort, 96 filter, 88 Select, 12 px gaps, wraps based on measured available width. Media stride is `card*9/16+170`; home uses one page. | `LibraryScene.qml` now has search/sort/filter/import; shared `StoneField`/`StoneButton`; card height follows 16:9+170. Select, pagination, overflow and full empty panel remain open. | Open: search/sort/filter route acceptance, selection and multi-action safety, responsive wrapping. |
| Library item detail | `library_detail_layout.py` and `app.py` include selected metadata, versions, description/tags/category, file/relink actions. | Annotation popup and basic file actions wired; full detail scene, variants and relink UI absent. | Open. |
| Library import | `app.py:_import_library_media`: native multi-file selection, max 64, `ArchiveWorkOwner` inspection, `commit_imports` durable history, failed-count result and operation telemetry. | Qt file picker, `ArchiveWorkOwner` inspection/commit and bounded operation telemetry wired; import blocks conflicting work. Native journey and conflict matrix pending. | Open. |
| Watch home | `watch_scene_ui.py:_scene_hero`: 36 px inset, 40/32 px title, chips, description, progress/resume, Play 150, Library 181, More 54; full hero height derives from content. For one item `_scene_small_library` puts Playlists and Channels side by side at width>=800. | `WatchScene.qml` now has a full-width hero and side-by-side Playlists/Channels; rendered at 1100×740. Chips/description/progress/More, exact hero height and responsive split are missing. | Open: matching client/DPI captures and features. |
| Watch routes | Tk channels/playlists/collections/videos, channel header, playlist detail, queue/shuffle, related media and search route. Cards follow `_card_width`, `_card_height` and 14 px stride. | `scene_projection.py` uses `watch_channels`, `watch_rails`, `unique_watch_videos`; Qt routes and group playback wired. Queue/shuffle, channel/playlist header, search, related actions and card art shapes remain open. | Open. |
| Player | Tk `player_scene_ui.py`/`media_player_ui.py`: playback, variants, preview moments, chapters, heatmap, details/related, window modes and queue. | `Main.qml` has current Qt Play/Pause, seek, volume, chapter and heatmap controls and Back to Watch. Preserve these newer controls. Other player surfaces and queue remain open. | Open: media-format/seek/resize/package gates. |
| Activity | Tk durable run timeline and detail modes via `activity_ui.py`/`forge_activity_ui.py`. | Qt runtime recent rows only. | Open. |
| Settings and editorial | Tk `focus_settings.py`, `engagement_ui.py`, `whats_new_ui.py`, `support_ui.py`: settings, consent, tour, What's New/Did You Know preview renderer, help/feedback, update/repair. | Qt settings/consent/update core present; editorial renderer and help/support not ported. | Open. |
| Telemetry and quality harness | Tk owner emits consent-gated feature, import, presentation, player, updater and diagnostic observations; engineering harness binds source/native/package journeys. | Qt telemetry has app/settings/download/player/update core but no full event parity ledger or native visual transition matrix for all routes. | Open: exact payload/readback/privacy and Mac/Genesis package acceptance. |
| QML packaging | `build_macos.sh`, `build_windows.ps1` and their release wrappers build platform candidates. | Build scripts now enumerate every `.qml` scene, including Library, Watch and Run Deck; previous fixed list omitted them. | Open: inspect actual signed Mac and Windows packages and launch every imported scene. |
| Material ownership | `ui_chrome.py`, `ui_button_contract.py` and `ui_materials.py` own etched controls; actual consumer layout and label still matter. | Qt `Materials`, `StoneButton`, `StoneField` now render buttons, fields and modal backgrounds; field height bound covers modal dimensions. | Open: actual material-to-screen evidence across states and DPI; source ownership regression passes. |

## Visual evidence already captured

- Current Tk 14-route sheet: `build/qt-port-package/tk-reference-d67/`.
- Current Tk 23-detail-state sheet: `build/qt-port-package/tk-detail-d67-v2/`.
- Current Qt source Watch and Library captures: `build/qt-port-package/{watch,library}-source-current.png`.

The current Qt captures verify the scenes render and expose real geometry gaps.
They are not a complete visual acceptance matrix. For each row above, compare
the implementation path, emitted actions, actual rendered bounds at matched
client dimensions, and the resulting state. Do not substitute a Tk screenshot
for newer intended Qt controls or copy a broken Tk rendering artifact.
