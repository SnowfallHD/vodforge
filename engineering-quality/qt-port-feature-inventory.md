# Qt port feature inventory

Source: current Tk code at `d67b11c8297dff409b05944357f0b8b8bd46ebc0`, inspected on 2026-09-23. The Tk screenshots are a **state and feature index**, not a pixel oracle: preserve the newer intended Qt concave design and controls where the Tk render is old or broken. The source owners below define behavior and reachability. The Qt column describes the current port, not release qualification.

## Shared shell and Forge

| Feature in Tk | Tk owner | Qt state |
| --- | --- | --- |
| Four views, brand, global search, navigation, responsive background cover | `app.py` focus chrome and view selection | Qt measured header now stacks one shared navigation row at narrow widths; 820×560 AX bounds pass, while full geometry comparison remains open. Full-cover artwork remains PreserveAspectCrop. |
| URL entry, format choice, presets, output destination, settings, download and queue | `app.py` Forge composer and submission | Core actions present; composer layout now being translated from current Tk. |
| Batch URL list and single/playlist control | `app.py`, `url_list_inputs.py` | Present at core level; verify visual and validation states. |
| Local audio and image to video conversion | `local_audio_video_ui.py` | Core action and Qt dialog present; visual and packaged journey pending. |
| Live title/artwork/progress, friendly/technical activity, source/output details | `app.py`, `forge_activity_ui.py`, `detail_ui.py` | Qt now reads the same run-owned friendly projection and current-run technical lines, with a rendered source toggle; title/artwork, complete output facts and detail dialogs remain open. |
| Run Deck: active, queued, terminal, preview and completed cards; overflow, actions, retry/skip/cancel | `app.py` `_focus_run_records`, `run_hover_menu.py`, `run_deck_renderer.py`, `library_state.py` | Qt has a rendered visual deck, completed-history projection and transient metadata preview card; `RunDeck.qml` exposes current action types. Only the four visible deck entries now request artwork; saved-run selection, all actions and responsive geometry still need code and native parity review. |

## Library and Watch

| Feature in Tk | Tk owner | Qt state |
| --- | --- | --- |
| Library sidebar and counts: All Media, Channels, Playlists, Videos, Audio | `library_scene_layout.py`, `library_scene_ui.py`, `watch_library.py` | Qt sidebar and routes rendered; responsive bounds and details remain open. |
| Folders, All media, Runs & previews, locations, paging and saved export versions | `archive_browser.py`, `archive_browser_ui.py`, `archive_library_ui.py` | Qt reads the existing metadata-only `ArchiveBrowserModel` for all three modes and location navigation, with a 48-item page and root recent-export cards. A grouped MP4/MP3 source test selects both versions and returns to the same folder; 1100×740 Qt source folder capture was inspected and 820×560 AX navigation bounds pass. Verified exact-file relink and missing-media recovery are now wired. Activity/preview activation, folder-wide relink, native package behavior and exact card geometry remain open. |
| Library home category cards, collections, recent media, artwork and storage tile | `library_scene_layout.py`, `library_scene_ui.py`, `volume_storage.py` | Qt scene, artwork, collection cards and shared capacity owner wired. Genesis packaged capture exposed “Drive unavailable”; direct source probe found Windows discovery used doubled drive-root slashes that could not equal the selected default root. Shared volume discovery now uses one slash with a 26-letter root regression. Rebuilt Genesis package and native tile readback remain open. |
| Search, sort, type/category filters, selection and paging/scroll position | `library_scene_ui.py`, `library_scene_layout.py` | Search, sort and category filter routes are wired. Select mode now offers collection, multi-file move and delete through shared controls and existing durable owners; source interaction and two-file transaction tests pass. All-media QML now windows card creation and artwork to viewport rows; a 200-item source test covers bounded requests, detail-return scroll restoration and sort/route reset. Native multi-file/scroll journeys and matched geometry remain open. |
| Library item details, output details, source variants, notes/tags/category | `library_detail_layout.py`, `library_scene_facts.py`, `library_annotation_ui.py` | Qt detail reads shared source/output facts and saved annotations. Description and direct tag chips/addition save through the existing annotation owner with a current-detail identity guard; notes/category also use the organization popup. Fact values copy current saved metadata and Source URL opens only a current HTTP(S) fact. Saved-version controls use the folder browser's grouped owners and retain the folder Back context. The 1100×740 detail source capture predates fact/version controls; relink and other complete detail actions remain open. |
| Create collection and assign media to it | `library_collection_ui.py`, `app.py` `_show_library_collection_editor` | Qt editor persists membership through shared annotations and both scenes read that projection. The Qt save now enforces Tk's 120-character name limit and records the existing consent-gated category action only after durable success. Source interaction passes; native dialog geometry and package journey remain open. |
| Import local media | `app.py` `_import_library_media`, `library_import.py` | Qt native picker, shared inspection and durable commit wired; package/native journey remains open. |
| Open saved location; remove card; move to folder/Trash; relink/recovery | `library_file_actions_ui.py`, `library_media_recovery_ui.py`, `archive_relink.py`, `app.py` | Open/remove/move/Trash and exact-file verified relink use the existing owners. Missing media now offers Locate, saved-profile redownload, or session-only Open in Forge. The relink handoff blocks competing Qt history writers and rejects a changed raw history document. Source popup and bounded tests passed; folder-wide mapping/relink, native dialogs/package journey and telemetry readback remain open. |
| Watch home hero, playlists/channels/collections rails and routes | `watch_scene_ui.py`, `watch_library.py`, `watch_ui.py`, `library_artwork_source.py` | Qt browse scene now projects the shared hero metadata and saved progress, source-role artwork for media/playlist/avatar/banner, and channel header counts/description. Full-cover Qt hero and channel source captures exist. Home Recent media is now one current-width row; the full Videos grid creates only viewport rows and requests artwork on demand. A 200-item QML test covers bounded cards/artwork and Back scroll restoration. The invalid-media accessibility gate now waits for the decoder failure and confirms the transport remains inside the minimum window after the error text appears. Group card windowing, related content, native scroll acceptance and responsive geometry remain open. |
| Watch queue, shuffle, back/search context and related media | `watch_scene_ui.py`, `player_scene_ui.py`, `watch_queue.py` | Group queue and shuffle now reach the shared queue owner. A source Qt run with two real local audio files advanced through both items and completed. Search filters through the shared Watch rail owner; a bounded route stack restores results on Back. Source Back scroll restoration now passes for a 200-item Videos route; native scroll/selection restoration and related media remain open. |
| Playback controls, volume, chapters, heatmap, preview moments, details, window modes | `media_player_ui.py`, `player_scene_ui.py`, `player_presentation_ui.py`, `player_related.py` | `PlayerScene.qml` keeps the current control material and per-open provider generation, and reads the shared Up Next/recent plan, identity, annotations and source/output facts. It exposes Edit your details and related Play/Details actions. Five midpoint thumbnails use the existing offline `MediaPreviewOwner` through a bounded Qt session; replacement retires prior results and moment buttons use shared seek. Fullscreen/floating/fit/fill switch the one Qt `MediaPlayer` between embedded and separate-window `VideoOutput` surfaces. Source QML tests verify output rebinding in all three modes. Correctly resized 1280×800 and 820×560 captures after the change show the static video stage and all transport/mode controls in one row. The new shared `CaptionTracks.qml` menu selects real Qt subtitle tracks in embedded and separate windows, displays `QVideoSink.subtitleText` over each surface, and keeps caption playback in Fit. An actual generated subtitle-bearing MP4 produced caption text in both QML overlays; offscreen Qt did not paint the video frame, so actual video, close/queue/native presentation and caption visual behavior still need package proof. Complete telemetry parity remains open. |

## Activity, help and release surfaces

| Feature in Tk | Tk owner | Qt state |
| --- | --- | --- |
| Activity timeline/log and run history | `activity_ui.py`, `forge_activity_ui.py`, `app.py` | Qt now uses the shared private log owner and current-run event binding; token typography, selected-run detail and package journey remain open. |
| Settings including output, format, network, cookies and analytics | `focus_settings.py`, `app.py` bindings | Qt Settings has the two-column source/output layout, session extra tags in canonical jobs, MP4/MP3/Original settings, live appearance through `ThemeRenderOwner`, analytics and Help/update actions. Preview metadata shares Tk's provider operation and gives Forge/Run Deck a transient card with Start download; Library Runs & previews activation and manual-selection guard are tested. The shared VODForge PRO button is rendered in the Settings header and routes through the existing cloud impression/click owners. Source preview, MP4/MP3/Cobalt/PRO captures were inspected. Manual MP4 fields now appear inline and the Settings source/output columns stack at narrow widths; QML interaction covers the eight-choice encoding-speed sequence. Actual network/native/D1 proof and matched native client/DPI geometry remain open. |
| Consent and privacy | `analytics_consent_ui.py`, `analytics_startup.py`, `install_attribution.py` | Qt consent core and popup present; the existing first-launch attribution owner now runs only after allowed consent, once per session. Denied/allowed source tests pass; visual/first-run and D1 readback journeys remain open. |
| Welcome tour, What's New, Did You Know (editorial mode currently `none`) | `engagement_ui.py`, `engagement_state.py`, `whats_new.py`, `whats_new_ui.py`, `whats_new_feature_preview.py` | Qt now uses `EngagementState` for first-run/rating eligibility and the shared highlight data/preview enum for a native QML carousel. Every declared preview key has a QML renderer using shared controls. The welcome popup was rendered and inspected; optional showcase mode is still `none`. All slide variants, automatic foreground sequencing and exact feature preview controls need rendered/package checks. |
| Help menu, feedback, review and support diagnostics | `engagement_ui.py`, `support_ui.py`, `support_payload.py`, `support_transport.py` | Qt Help button, shared-control feedback/review forms, reason/rating/message/reply and explicit diagnostics/video-URL consent now use one shared payload validator and the existing transport. Recent failed-run context is projected read-only into feedback. Wide/narrow source forms rendered; live delivery, verification handoff and full Help menu actions remain open. |
| Update check/download/install/repair and recovery | `app.py`, `updates.py`, `qt_quick/update_session.py` | Core Qt updater and popup present; exact packaged update/repair proof pending. |

## Capture coverage

- `build/qt-port-package/tk-reference-d67/manifest.json`: 14 real Tk route captures using isolated saved media.
- `build/qt-port-package/tk-detail-d67-v2/detail-manifest.json`: focused detail-state matrix, including both player formats, annotation/collection editors, settings, announcements, consent and native menus/alerts. See each case's outcome; a capture is evidence of a rendered state only.
- Still to capture or exercise before claiming parity: Library item detail and local import, Watch playlist/channel detail and queue, player window modes and related content, file move/relink/recovery dialogs, update install/repair states, help/support, empty and error states, narrow/large window layouts, Windows packaged captures.

Qt release status: **blocked on feature and visual parity plus packaged Mac/Windows journeys**. Source tests, screenshots, and runtime smoke do not by themselves qualify a release.

The complete code-file source index is generated at `build/qt-port-package/source-index-current.json`. `qt-port-owner-review.md` lists every current Tk presentation file, including indirect scene mixins; a harness test requires new Tk presentation files to enter that review. This file coverage is an audit boundary, not completed feature parity.
